{
  "name": "claude-code-monitor",
  "version": "0.1.0",
  "description": "Monitor multiple Claude Code session states",
  "type": "module",
  "bin": {
    "claude-code-monitor": "./bin/claude-code-monitor"
  },
  "scripts": {
    "build": "bun build --compile --minify src/cli.ts --outfile bin/.compiled",
    "typecheck": "bun run --bun tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "cli-table3": "^0.6.5"
  }
}
