{
  "name": "claude-watchdog",
  "version": "0.1.0",
  "description": "Monitor multiple Claude Code session states",
  "bin": {
    "claude-watchdog": "./dist/bin/claude-watchdog"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "moon build --target native && node scripts/copy-binary.js",
    "test": "moon test --target native"
  }
}
